# Pterodactyl Backend
Runs Telegram bot + API.

Install:
pip install -r requirements.txt

Run:
python main.py

API:
http://your-host:port/api/wiki

For Mini App real-time updates, the API must be public HTTPS.
